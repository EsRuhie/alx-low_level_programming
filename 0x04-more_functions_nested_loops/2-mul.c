#include "main.h"
#include <stdio.h>

/**
 * mul - function to multiply two integers and return the result
 * @a: - The first integer
 * @b: - The second integer
 * Return: Always 0
 */

int mul(int a, int b)
{
int result = a * b;
return (result);
}
